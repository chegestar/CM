
#ifndef __CODES_H__
#define __CODES_H__

typedef enum INVENTORY_CODE {
  NO_ITEM=-1,
  FIRE_BOOT,
  DYNAMITE,
  MAX_INVENTORY
} I_CODE;

typedef enum LEVEL_TYPE {
  NORMAL,
  GRID,
  CONTINUOUS,
  AUTOSCROLL
} L_TYPE;

#endif
